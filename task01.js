"use strict";

const PRODUCT_NAME = "Генератор защитного поля ";
let productPrice = 1000;

console.log(`Выбран ${PRODUCT_NAME}, цена за штуку ${productPrice} `);

productPrice = 2000;

console.log(`Выбран ${PRODUCT_NAME}, цена за штуку ${productPrice} `);
